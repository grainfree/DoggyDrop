@model DoggyDrop.ViewModels.CommunityViewModel

@{
    ViewData["Title"] = "Skupnost";
}

<section class="app-page community-page">
    <header class="app-page__header community-hero">
        <div>
            <span class="community-hero__eyebrow">DoggyDrop live</span>
            <h1 class="app-page__title">Skupnost</h1>
            <p class="app-page__lead">Nedavni sprehodi, aktivni psi in bolj živ DoggyDrop community feed.</p>
        </div>
        <div class="community-hero__actions">
            <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Playdates" asp-action="Index">Playdates</a>
            <a class="btn btn-outline-secondary rounded-pill fw-semibold" asp-controller="Friends" asp-action="Index">Prijatelji</a>
        </div>
        <div class="community-hero__pulse" aria-label="Povzetek skupnosti">
            <article>
                <strong>@Model.KilometersThisWeek.ToString("0.0")</strong>
                <span>km ta teden</span>
            </article>
            <article>
                <strong>@Model.ActiveDogsThisWeek</strong>
                <span>aktivnih psov</span>
            </article>
        </div>
    </header>

    <div class="community-stats">
        <article>
            <strong>@Model.KilometersThisWeek.ToString("0.0") km</strong>
            <span>Ta teden</span>
        </article>
        <article>
            <strong>@Model.WalksThisWeek</strong>
            <span>Sprehodi</span>
        </article>
        <article>
            <strong>@Model.ActiveDogsThisWeek</strong>
            <span>Aktivni psi</span>
        </article>
    </div>

    <section class="community-section community-local-board">
        <div class="community-section__header">
            <h2>Lokalne lestvice</h2>
            <span>@Model.LocalLeaderboards.CityName</span>
        </div>
        <div class="community-local-grid">
            <article>
                <h3>Most distance</h3>
                @foreach (var entry in Model.LocalLeaderboards.MostDistance)
                {
                    <div class="community-local-entry">
                        <strong>@entry.Rank</strong>
                        <span>@entry.Label <small>@entry.SubLabel</small></span>
                        <em>@entry.ScoreText</em>
                    </div>
                }
            </article>
            <article>
                <h3>Most discoveries</h3>
                @foreach (var entry in Model.LocalLeaderboards.MostDiscoveries)
                {
                    <div class="community-local-entry">
                        <strong>@entry.Rank</strong>
                        <span>@entry.Label <small>@entry.SubLabel</small></span>
                        <em>@entry.ScoreText</em>
                    </div>
                }
            </article>
            <article>
                <h3>Most helpful</h3>
                @foreach (var entry in Model.LocalLeaderboards.MostHelpful)
                {
                    <div class="community-local-entry">
                        <strong>@entry.Rank</strong>
                        <span>@entry.Label <small>@entry.SubLabel</small></span>
                        <em>@entry.ScoreText</em>
                    </div>
                }
            </article>
            <article>
                <h3>Best photos</h3>
                @foreach (var entry in Model.LocalLeaderboards.BestPhotos)
                {
                    <div class="community-local-entry">
                        <strong>@entry.Rank</strong>
                        <span>@entry.Label <small>@entry.SubLabel</small></span>
                        <em>@entry.ScoreText</em>
                    </div>
                }
            </article>
            <article>
                <h3>Top dog this week</h3>
                @foreach (var entry in Model.LocalLeaderboards.TopDogsThisWeek)
                {
                    <a class="community-local-entry" asp-controller="Dogs" asp-action="Details" asp-route-id="@entry.DogId">
                        <strong>@entry.Rank</strong>
                        <span>@entry.Label <small>@entry.SubLabel</small></span>
                        <em>@entry.ScoreText</em>
                    </a>
                }
            </article>
        </div>
    </section>

    <section class="community-section">
        <div class="community-section__header">
            <h2>Tedenska lestvica</h2>
            <span>Top psi</span>
        </div>

        @if (Model.WeeklyLeaders.Count == 0)
        {
            <article class="community-empty community-empty--founder">
                <h3>Be the first DoggyDrop Explorer here.</h3>
                <p>Zaključi prvi sprehod v svojem kraju in odpri tedensko lestvico, preden jo napolni skupnost.</p>
                <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Walks" asp-action="Planner">Začni prvi sprehod</a>
            </article>
        }
        else
        {
            <div class="community-leaderboard">
                @for (var i = 0; i < Model.WeeklyLeaders.Count; i++)
                {
                    var leader = Model.WeeklyLeaders[i];
                    <a class="community-leader" asp-controller="Dogs" asp-action="Details" asp-route-id="@leader.DogId">
                        <strong>@(i + 1)</strong>
                        <div class="community-avatar">
                            @if (!string.IsNullOrWhiteSpace(leader.DogPhotoUrl))
                            {
                                <img src="@leader.DogPhotoUrl" alt="@leader.DogName" />
                            }
                            else
                            {
                                <span>@leader.DogName.Substring(0, 1).ToUpperInvariant()</span>
                            }
                        </div>
                        <div>
                            <span>@leader.DogName</span>
                            <small>@leader.OwnerName · @leader.WalkCount sprehodov</small>
                        </div>
                        <em>@leader.WeeklyDistanceKm.ToString("0.0") km</em>
                    </a>
                }
            </div>
        }
    </section>

    <section class="community-section">
        <div class="community-section__header">
            <h2>Playdates</h2>
            <span>Srečanja psov</span>
        </div>
        <article class="community-empty">
            <h3>Povabi pse na sprehod</h3>
            <p>Ustvari playdate povabilo z lokacijo, energijo in velikostjo psov. To je prvi korak proti dog meetup funkciji.</p>
            <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Playdates" asp-action="Index">Odpri playdates</a>
        </article>
    </section>

    <section class="community-section">
        <div class="community-section__header">
            <h2>Feed prijateljev</h2>
            <span>Prijatelji</span>
        </div>

        @if (Model.FriendsWalks.Count == 0)
        {
            <article class="community-empty">
                <h3>Prijatelji se še niso oglasili</h3>
                <p>Ko bodo prijatelji zaključevali sprehode, se bodo tukaj pojavili njihovi povzetki in fotke.</p>
                <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Friends" asp-action="Index">Odpri prijatelje</a>
            </article>
        }
        else
        {
            <div class="community-feed">
                @foreach (var walk in Model.FriendsWalks)
                {
                    <a class="community-feed-card community-feed-card--friend" asp-controller="Walks" asp-action="Details" asp-route-id="@walk.WalkId">
                        <div class="community-avatar community-avatar--large">
                            @if (!string.IsNullOrWhiteSpace(walk.DogPhotoUrl))
                            {
                                <img src="@walk.DogPhotoUrl" alt="@walk.DogName" />
                            }
                            else
                            {
                                <span>@walk.DogName.Substring(0, 1).ToUpperInvariant()</span>
                            }
                        </div>
                        <div class="community-feed-card__body">
                            <div class="community-feed-card__title">
                                <strong>@walk.DogName</strong>
                                <span>@walk.DistanceKm.ToString("0.0") km</span>
                            </div>
                            @if (!string.IsNullOrWhiteSpace(walk.CoverPhotoUrl))
                            {
                                <div class="community-feed-card__cover">
                                    <img src="@walk.CoverPhotoUrl" alt="Fotografija sprehoda @walk.DogName" />
                                </div>
                            }
                            <p>@walk.OwnerName je bil na sprehodu @walk.StartedAt.ToLocalTime().ToString("dd.MM. HH:mm").</p>
                            <div class="community-feed-card__meta">
                                <span>@walk.UsedBinsCount koši</span>
                                <span>@walk.LikeCount tačk</span>
                                <span>@walk.CommentCount komentarjev</span>
                                <span>@walk.PhotoCount foto</span>
                                <span>Odpri povzetek</span>
                            </div>
                        </div>
                    </a>
                }
            </div>
        }
    </section>

    <section class="community-section">
        <div class="community-section__header">
            <h2>Photo feed</h2>
            <span>Sprehodi v slikah</span>
        </div>

        @if (Model.PhotoFeed.Count == 0)
        {
            <article class="community-empty community-empty--founder">
                <h3>Prva fotka čaka nate</h3>
                <p>Dodaj walk photo in postani prvi obraz lokalnega DoggyDrop feeda.</p>
                <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Walks" asp-action="Index">Odpri sprehode</a>
            </article>
        }
        else
        {
            <div class="community-photo-feed">
                @foreach (var photo in Model.PhotoFeed)
                {
                    <article class="community-photo-card">
                        <img src="@photo.ImageUrl" alt="Fotografija sprehoda @photo.DogName" />
                        <div class="community-photo-card__overlay">
                            <strong>@photo.DogName</strong>
                            <span>@photo.OwnerName</span>
                            @if (!string.IsNullOrWhiteSpace(photo.StopName))
                            {
                                <small>@photo.StopName</small>
                            }
                            <small>@(string.IsNullOrWhiteSpace(photo.Caption) ? photo.CreatedAt.ToLocalTime().ToString("dd.MM. HH:mm") : photo.Caption)</small>
                            <div class="community-reaction-strip">
                                @foreach (var reaction in new[] {
                                    new { Type = "paw", Label = "🐾" },
                                    new { Type = "heart", Label = "♥" },
                                    new { Type = "fire", Label = "🔥" },
                                    new { Type = "cute-dog", Label = "Cute dog" }
                                })
                                {
                                    var count = photo.ReactionCounts.TryGetValue(reaction.Type, out var reactionCount) ? reactionCount : 0;
                                    <form class="community-photo-card__reaction" asp-controller="Walks" asp-action="TogglePhotoReaction" asp-route-id="@photo.WalkId" asp-route-photoId="@photo.PhotoId" method="post">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="reactionType" value="@reaction.Type" />
                                        <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                        <button type="submit" class="@(photo.CurrentUserReactionType == reaction.Type ? "is-liked" : string.Empty)">@reaction.Label @count</button>
                                    </form>
                                }
                            </div>
                        </div>
                    </article>
                }
            </div>
        }
    </section>

    <section class="community-section">
        <div class="community-section__header">
            <h2>Bin gallery</h2>
            <span>Pasji koši s fotkami</span>
        </div>

        @if (Model.BinPhotoGallery.Count == 0)
        {
            <article class="community-empty community-empty--founder">
                <h3>Be the first bin founder.</h3>
                <p>Dodaj prvi koš s fotografijo v svoji okolici. Če je to prvo odobreno območje, dobiš trajen founder badge.</p>
                <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Map" asp-action="Add">Dodaj koš</a>
            </article>
        }
        else
        {
            <div class="community-bin-gallery">
                @foreach (var bin in Model.BinPhotoGallery)
                {
                    <a class="community-bin-card" asp-controller="Map" asp-action="Index" asp-fragment="bin-@bin.BinId">
                        <img src="@bin.ImageUrl" alt="@bin.BinName" />
                        <div>
                            <strong>@bin.BinName</strong>
                            <span>@(string.IsNullOrWhiteSpace(bin.ContributorName) ? "DoggyDrop skupnost" : bin.ContributorName)</span>
                        </div>
                    </a>
                }
            </div>
        }
    </section>

    <section class="community-section">
        <div class="community-section__header">
            <h2>Feed sprehodov</h2>
            <span>Najnovejše</span>
        </div>

        @if (Model.RecentWalks.Count == 0)
        {
            <article class="community-empty community-empty--founder">
                <h3>Be the first DoggyDrop Explorer here.</h3>
                <p>Zaključi sprehod in DoggyDrop ga bo prikazal kot prvo aktivnost tvoje skupnosti.</p>
                <a class="btn btn-success rounded-pill fw-semibold" asp-controller="Walks" asp-action="Index">Začni sprehod</a>
            </article>
        }
        else
        {
            <div class="community-feed">
                @foreach (var walk in Model.RecentWalks)
                {
                    <a class="community-feed-card" asp-controller="Walks" asp-action="Details" asp-route-id="@walk.WalkId">
                        <div class="community-avatar community-avatar--large">
                            @if (!string.IsNullOrWhiteSpace(walk.DogPhotoUrl))
                            {
                                <img src="@walk.DogPhotoUrl" alt="@walk.DogName" />
                            }
                            else
                            {
                                <span>@walk.DogName.Substring(0, 1).ToUpperInvariant()</span>
                            }
                        </div>
                        <div class="community-feed-card__body">
                            <div class="community-feed-card__title">
                                <strong>@walk.DogName</strong>
                                <span>@walk.DistanceKm.ToString("0.0") km</span>
                            </div>
                            @if (!string.IsNullOrWhiteSpace(walk.CoverPhotoUrl))
                            {
                                <div class="community-feed-card__cover">
                                    <img src="@walk.CoverPhotoUrl" alt="Fotografija sprehoda @walk.DogName" />
                                </div>
                            }
                            <p>@walk.OwnerName je zaključil sprehod @walk.StartedAt.ToLocalTime().ToString("dd.MM. HH:mm").</p>
                            <div class="community-feed-card__meta">
                                <span>@walk.UsedBinsCount koši</span>
                                <span>@walk.LikeCount tačk</span>
                                <span>@walk.CommentCount komentarjev</span>
                                <span>@walk.PhotoCount foto</span>
                                <span>Odpri povzetek</span>
                            </div>
                        </div>
                    </a>
                }
            </div>
        }
    </section>
</section>

<style>
    .community-stats {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 14px;
        margin-bottom: 14px;
    }

    .community-hero {
        display: flex;
        align-items: flex-end;
        justify-content: space-between;
        gap: 16px;
    }

    .community-hero__actions {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
    }

    .community-stats article,
    .community-section,
    .community-empty,
    .community-feed-card {
        border: 1px solid rgba(31, 45, 41, 0.08);
        border-radius: 20px;
        background: linear-gradient(180deg, rgba(255,255,255,0.98), rgba(249,252,247,0.96));
        box-shadow: 0 18px 34px rgba(31, 45, 41, 0.08);
    }

    .community-stats article {
        padding: 18px;
    }

    .community-stats strong,
    .community-stats span {
        display: block;
    }

    .community-stats strong {
        font-size: 1.35rem;
        font-weight: 950;
    }

    .community-stats span,
    .community-muted,
    .community-empty p {
        color: #65716d;
    }

    .community-section {
        margin-top: 14px;
        padding: 20px;
    }

    .community-empty--founder {
        border-color: rgba(239, 143, 66, 0.22);
        background: linear-gradient(180deg, #fff9ee, #ffffff);
        box-shadow: 0 18px 34px rgba(239, 143, 66, 0.10);
    }

    .community-empty--founder h3::before {
        content: "Founder";
        display: inline-flex;
        margin-right: 8px;
        padding: 4px 8px;
        border-radius: 999px;
        background: rgba(239, 143, 66, 0.14);
        color: #b76519;
        font-size: 0.68rem;
        font-weight: 950;
        text-transform: uppercase;
        vertical-align: middle;
    }

    .community-section__header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 12px;
    }

    .community-section__header h2,
    .community-empty h3 {
        margin: 0;
        font-size: 1.1rem;
        font-weight: 950;
    }

    .community-section__header span {
        color: #2f6d5f;
        font-size: 0.82rem;
        font-weight: 850;
    }

    .community-leaderboard,
    .community-feed {
        display: grid;
        gap: 12px;
    }

    .community-photo-feed {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 12px;
    }

    .community-photo-card,
    .community-bin-card {
        position: relative;
        overflow: hidden;
        border-radius: 18px;
        color: inherit;
        text-decoration: none;
        min-height: 220px;
    }

    .community-photo-card img,
    .community-bin-card img,
    .community-feed-card__cover img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .community-photo-card__overlay {
        position: absolute;
        inset: auto 0 0 0;
        display: grid;
        gap: 4px;
        padding: 14px 12px 12px;
        background: linear-gradient(180deg, rgba(12, 25, 22, 0), rgba(12, 25, 22, 0.76));
        color: #ffffff;
    }

    .community-photo-card__overlay strong,
    .community-bin-card strong {
        font-weight: 950;
    }

    .community-photo-card__overlay span,
    .community-photo-card__overlay small,
    .community-bin-card span {
        opacity: 0.88;
    }

    .community-reaction-strip {
        display: flex;
        flex-wrap: wrap;
        gap: 6px;
        margin-top: 4px;
    }

    .community-photo-card__reaction {
        margin: 0;
    }

    .community-photo-card__reaction button {
        min-height: 34px;
        padding: 7px 10px;
        border: 0;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.16);
        color: #ffffff;
        font-size: 0.78rem;
        font-weight: 900;
    }

    .community-photo-card__reaction .is-liked {
        background: #ffffff;
        color: #2f6d5f;
    }

    .community-bin-gallery {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 12px;
    }

    .community-bin-card {
        display: grid;
        grid-template-rows: 180px auto;
        min-height: 0;
        border: 1px solid rgba(31, 45, 41, 0.1);
        background: #ffffff;
        box-shadow: 0 14px 30px rgba(31, 45, 41, 0.08);
    }

    .community-bin-card div {
        display: grid;
        gap: 4px;
        padding: 12px;
    }

    .community-local-grid {
        display: grid;
        grid-template-columns: repeat(5, minmax(0, 1fr));
        gap: 12px;
    }

    .community-local-grid article {
        display: grid;
        align-content: start;
        gap: 8px;
        min-width: 0;
        padding: 12px;
        border: 1px solid rgba(31, 45, 41, 0.08);
        border-radius: 8px;
        background: #ffffff;
    }

    .community-local-grid h3 {
        margin: 0 0 4px;
        color: #1f2d29;
        font-size: 0.9rem;
        font-weight: 950;
    }

    .community-local-entry {
        display: grid;
        grid-template-columns: 24px 1fr;
        gap: 8px;
        align-items: start;
        color: inherit;
        text-decoration: none;
    }

    .community-local-entry > strong {
        display: grid;
        width: 24px;
        aspect-ratio: 1;
        place-items: center;
        border-radius: 999px;
        background: #eef7ed;
        color: #2f6d5f;
        font-size: 0.78rem;
        font-weight: 950;
    }

    .community-local-entry span,
    .community-local-entry small,
    .community-local-entry em {
        display: block;
        min-width: 0;
    }

    .community-local-entry span {
        color: #1f2d29;
        font-size: 0.84rem;
        font-weight: 900;
    }

    .community-local-entry small,
    .community-local-entry em {
        color: #65716d;
        font-size: 0.76rem;
        font-weight: 800;
    }

    .community-local-entry em {
        grid-column: 2;
        color: #2f6d5f;
        font-style: normal;
    }

    .community-leader,
    .community-feed-card {
        color: inherit;
        text-decoration: none;
    }

    .community-leader {
        display: grid;
        grid-template-columns: 28px 48px 1fr auto;
        gap: 12px;
        align-items: center;
    }

    .community-leader > strong {
        color: #2f6d5f;
        font-size: 1.25rem;
        font-weight: 950;
    }

    .community-leader span,
    .community-leader small {
        display: block;
    }

    .community-leader span {
        font-weight: 900;
    }

    .community-leader small,
    .community-feed-card p {
        color: #65716d;
    }

    .community-leader em {
        color: #2f6d5f;
        font-style: normal;
        font-weight: 950;
    }

    .community-avatar {
        display: grid;
        width: 48px;
        aspect-ratio: 1;
        place-items: center;
        overflow: hidden;
        border-radius: 16px;
        background: linear-gradient(135deg, #e7f3e5, #fff3dd);
        color: #2f6d5f;
        font-size: 1.25rem;
        font-weight: 950;
    }

    .community-avatar--large {
        width: 68px;
    }

    .community-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .community-feed-card {
        display: grid;
        grid-template-columns: 68px 1fr;
        gap: 14px;
        padding: 14px;
    }

    .community-feed-card--friend {
        background: linear-gradient(180deg, #ffffff, #f7fbf8);
    }

    .community-feed-card:hover,
    .community-feed-card:focus,
    .community-leader:hover,
    .community-leader:focus {
        color: #2f6d5f;
    }

    .community-feed-card__title,
    .community-feed-card__meta {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
    }

    .community-feed-card__cover {
        margin: 8px 0 10px;
        overflow: hidden;
        border-radius: 16px;
        aspect-ratio: 16 / 9;
    }

    .community-feed-card__title strong {
        font-size: 1.05rem;
        font-weight: 950;
    }

    .community-feed-card__title span,
    .community-feed-card__meta span {
        color: #2f6d5f;
        font-weight: 850;
    }

    .community-feed-card p {
        margin: 4px 0 10px;
    }

    .community-feed-card__meta {
        font-size: 0.84rem;
    }

    .community-empty {
        padding: 22px;
    }

    .community-empty p {
        margin: 8px 0 14px;
    }

    @@media (max-width: 575.98px) {
        .community-stats {
            grid-template-columns: 1fr;
        }

        .community-hero,
        .community-hero__actions,
        .community-section__header,
        .community-feed-card__title,
        .community-feed-card__meta {
            align-items: stretch;
            flex-direction: column;
        }

        .community-leader,
        .community-feed-card,
        .community-local-grid,
        .community-photo-feed,
        .community-bin-gallery {
            grid-template-columns: 1fr;
        }

        .community-avatar,
        .community-avatar--large {
            width: 100%;
            max-width: 120px;
        }
    }

    .community-page {
        max-width: 1120px;
        padding-bottom: 34px;
    }

    .community-page .community-hero {
        position: relative;
        display: grid;
        grid-template-columns: minmax(0, 1fr) auto minmax(230px, 0.45fr);
        align-items: center;
        overflow: hidden;
        padding: 24px;
        border: 1px solid rgba(20, 40, 35, 0.09);
        border-radius: 16px;
        background:
            radial-gradient(circle at 16% 16%, rgba(232, 137, 62, 0.13), transparent 24%),
            linear-gradient(135deg, rgba(255, 255, 255, 0.98), rgba(229, 242, 235, 0.96));
        box-shadow: 0 18px 38px rgba(20, 40, 35, 0.08);
    }

    .community-page .community-hero::after {
        position: absolute;
        right: -100px;
        bottom: -130px;
        width: 280px;
        height: 280px;
        border-radius: 999px;
        background: rgba(36, 105, 90, 0.08);
        content: "";
    }

    .community-hero__eyebrow {
        display: inline-flex;
        width: fit-content;
        margin-bottom: 10px;
        padding: 7px 11px;
        border-radius: 999px;
        background: #e3f0e9;
        color: #24695a;
        font-size: 0.78rem;
        font-weight: 950;
    }

    .community-page .community-hero .app-page__title {
        color: #142823;
        font-size: clamp(2rem, 5vw, 3.5rem);
        letter-spacing: 0;
    }

    .community-page .community-hero .app-page__lead {
        max-width: 620px;
    }

    .community-page .community-hero__actions {
        position: relative;
        z-index: 1;
        justify-content: flex-end;
    }

    .community-page .community-hero__actions .btn {
        min-height: 44px;
        border-radius: 12px !important;
        padding-inline: 16px;
    }

    .community-hero__pulse {
        position: relative;
        z-index: 1;
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 10px;
    }

    .community-hero__pulse article {
        min-width: 0;
        padding: 14px 12px;
        border: 1px solid rgba(20, 40, 35, 0.08);
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.78);
        text-align: center;
    }

    .community-hero__pulse strong,
    .community-hero__pulse span {
        display: block;
    }

    .community-hero__pulse strong {
        color: #142823;
        font-size: 1.55rem;
        font-weight: 950;
        line-height: 1;
    }

    .community-hero__pulse span {
        margin-top: 4px;
        color: #5f706a;
        font-size: 0.76rem;
        font-weight: 900;
        text-transform: uppercase;
    }

    .community-page .community-stats {
        grid-template-columns: repeat(3, minmax(0, 1fr));
        margin: 14px 0;
    }

    .community-page .community-stats article,
    .community-page .community-section,
    .community-page .community-empty,
    .community-page .community-feed-card {
        border-color: rgba(20, 40, 35, 0.09);
        border-radius: 16px;
        background: rgba(255, 255, 255, 0.96);
        box-shadow: 0 18px 38px rgba(20, 40, 35, 0.08);
    }

    .community-page .community-stats article {
        padding: 16px;
    }

    .community-page .community-stats strong {
        color: #142823;
        font-size: 1.2rem;
    }

    .community-page .community-section {
        padding: 20px;
    }

    .community-page .community-section__header h2 {
        color: #142823;
        font-size: 1.18rem;
    }

    .community-page .community-section__header span {
        color: #24695a;
    }

    .community-page .community-local-grid {
        grid-template-columns: repeat(5, minmax(170px, 1fr));
        overflow-x: auto;
        padding-bottom: 4px;
    }

    .community-page .community-local-grid article,
    .community-page .community-local-entry > strong,
    .community-page .community-empty,
    .community-page .community-photo-card,
    .community-page .community-bin-card,
    .community-page .community-avatar,
    .community-page .community-feed-card__cover {
        border-radius: 12px;
    }

    .community-page .community-local-grid article {
        background: #f8fbf8;
    }

    .community-page .community-photo-feed,
    .community-page .community-bin-gallery {
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    }

    .community-page .community-photo-card {
        min-height: 260px;
        box-shadow: 0 18px 34px rgba(20, 40, 35, 0.12);
    }

    .community-page .community-bin-card {
        grid-template-rows: 200px auto;
        box-shadow: 0 18px 34px rgba(20, 40, 35, 0.10);
    }

    .community-page .community-leader {
        padding: 10px;
        border: 1px solid rgba(20, 40, 35, 0.08);
        border-radius: 12px;
        background: #f8fbf8;
    }

    .community-page .community-feed-card {
        grid-template-columns: 72px minmax(0, 1fr);
        padding: 16px;
    }

    .community-page .community-avatar,
    .community-page .community-avatar--large {
        border-radius: 14px;
    }

    .community-page .community-feed-card__meta {
        flex-wrap: wrap;
        justify-content: flex-start;
    }

    .community-page .community-feed-card__meta span {
        display: inline-flex;
        min-height: 28px;
        align-items: center;
        padding: 5px 9px;
        border-radius: 999px;
        background: #e3f0e9;
        color: #24695a;
        font-size: 0.78rem;
    }

    .community-page .community-empty--founder {
        border-color: rgba(232, 137, 62, 0.24);
        background: linear-gradient(180deg, #fff8ee, #ffffff);
    }

    .community-page .community-empty .btn {
        border-radius: 12px !important;
    }

    @@media (max-width: 920px) {
        .community-page .community-hero {
            grid-template-columns: 1fr;
        }

        .community-page .community-hero__actions {
            justify-content: flex-start;
        }

        .community-hero__pulse {
            max-width: 420px;
        }
    }

    @@media (max-width: 575.98px) {
        .community-page {
            padding-bottom: 24px;
        }

        .community-page .community-hero {
            padding: 18px;
        }

        .community-page .community-hero .app-page__title {
            font-size: 2rem;
        }

        .community-hero__pulse,
        .community-page .community-stats {
            grid-template-columns: repeat(3, minmax(0, 1fr));
        }

        .community-hero__pulse article {
            padding: 12px 8px;
        }

        .community-hero__pulse strong {
            font-size: 1.1rem;
        }

        .community-hero__pulse span,
        .community-page .community-stats span {
            font-size: 0.68rem;
        }

        .community-page .community-stats strong {
            font-size: 0.95rem;
        }

        .community-page .community-feed-card {
            grid-template-columns: 1fr;
        }

        .community-page .community-avatar,
        .community-page .community-avatar--large {
            width: 72px;
        }

        .community-page .community-local-grid {
            grid-template-columns: 1fr;
            overflow-x: visible;
        }
    }
</style>
